{
  "name": "@gregcello/msw-server-helper",
  "version": "1.0.0",
  "description": "Dynamic modification of node mock service worker mocks from within browser-based tests like Cypress and Playwright",
  "main": "dist/index.js",
  "module": "esm/index.js",
  "scripts": {
    "build:prisma": "prisma generate --schema src/prisma/schema.prisma",
    "build:db": "prisma db push --schema src/prisma/schema.prisma",
    "build:commonjs": "tsc --module commonjs --outDir dist",
    "build:esm": "tsc --module esnext --outDir esm",
    "build": "pnpm run build:prisma && pnpm run build:db && pnpm run build:commonjs && pnpm run build:esm",
    "setup": "pnpm run build:prisma && pnpm run build:db"
  },
  "keywords": [],
  "author": "Gregory Beaver <greg.beaver.cello@gmail.com>",
  "license": "MIT",
  "peerDependencies": {
    "msw": ">= 0.49.2"
  },
  "dependencies": {
    "@prisma/client": "4.8.0",
    "prisma": "4.8.0"
  },
  "devDependencies": {
    "rimraf": "3.0.2",
    "typescript": "4.9.4"
  }
}
